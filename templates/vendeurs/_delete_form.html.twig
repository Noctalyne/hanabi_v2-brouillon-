<form method="post" action="{{ path('app_vendeurs_delete', {'id': vendeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vendeur.id) }}">
    <button class="btn">Delete</button>
</form>
