{% extends 'base.html.twig' %}

{% block title %}Vendeurs{% endblock %}

{% block body %}
    <h1>Vendeurs</h1>

    <table class="table">
        <tbody>

        <tr>
            <th>Id</th>
            <td>{{ vendeur.id }}</td>
        </tr>
            <th>Email</th>
            {# <td>{{ user.roles }}</td> #}
        </tr>
        <tr>
        <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Username</th>
            <td>{{ user.username }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ user.password }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ vendeur.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ vendeur.prenom }}</td>
        </tr>

        </tbody>
    </table>

    {% if app.user.vendeurs %}
        <a href="{{ path('app_vendeurs_index') }}">Retour à la liste</a>
    {% else %}
        <a href="{{ path('app_vendeurs_index') }}">Retour au profil</a>
    {% endif %}
    <a href="{{ path('app_vendeurs_edit', {'user_id': app.user.id}) }}">edit</a>

    {# {{ include('vendeurs/_delete_form.html.twig') }} #}
{% endblock %}
