{% extends 'base.html.twig' %}

{% block title %}Clients{% endblock %}

{% block body %}
    <h1>Clients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                {# <td>{{ client.roles ? client.roles|json_encode : '' }}</td> #}
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                {# <td>{{ client.password }}</td> #}
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                {# <td>{{ client.prenom }}</td> #}
            </tr>
            <tr>
                <th>Telephone</th>
                {# <td>{{ client.telephone }}</td> #}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_clients_index') }}">back to list</a>

    <a href="{{ path('app_clients_edit', {'user_id': app.user.id}) }}">edit</a>

    {# {{ include('clients/_delete_form.html.twig') }} #}
{% endblock %}
