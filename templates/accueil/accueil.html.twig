{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1> test avec foreignkey  </h1>
    {% if app.user %}
        Bonjour {{app.user.username}}

        <form method="post" action="{{ path('app_logout') }}">
            <button type="submit" class="logout">Déconnexion</button>
        </form>


        <a href="{{ path('app_clients_show', {'user_id' : app.user.id}) }}">Profil</a>
        

    {% else %}
    <button><a href="{{ path('app_register') }}">Inscription</a></button>
    <button><a href="{{ path('app_login') }}">Connexion</a></button>
    {% endif %}

    <div>
    <button><a href="{{ path('app_user_index') }}">liste user</a></button>
    
    <button><a href="{{ path('app_clients_index') }}">liste clients</a></button>
    </div>


</div>
{% endblock %}
