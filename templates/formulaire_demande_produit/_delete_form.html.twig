<form method="post" action="{{ path('app_formulaire_demande_produit_delete', {'id': formulaire_demande_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formulaire_demande_produit.id) }}">
    <button class="btn">Delete</button>
</form>
